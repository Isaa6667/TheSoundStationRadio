// Player configuration

window.RADIO_CONFIG = {
    // Radio station name
    RADIO_NAME: 'The Sound Station',

    // Stream URL
    URL_STREAMING: 'https://sound-station.volticast.net/primary',

    // Optional: Override metadata API URLs if required
    // API_URL: '',
    // FALLBACK_API_URL: '',

    // Optional: Override theme colors
    // ACCENT_COLOR: '#6D28D9',
    // BG_COLOR: '#0B0E13',
};
